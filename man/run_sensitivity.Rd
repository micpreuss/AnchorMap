% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{run_sensitivity}
\alias{run_sensitivity}
\title{Parallel reliability-threshold sensitivity sweep}
\usage{
run_sensitivity(
  df,
  ont,
  cfg,
  sroot,
  z_vector,
  threads = 1L,
  trait_rg_override = NULL,
  emit = message
)
}
\arguments{
\item{df}{Gated long-table data frame (Input A schema).}

\item{ont}{Ontology data frame (read by \code{read_ontology()}).}

\item{cfg}{Config list (from \code{load_config()}).}

\item{sroot}{Stage root for resolving relative paths.}

\item{z_vector}{Numeric vector of h2-reliability thresholds to sweep.}

\item{threads}{Worker count for the z-axis (uses \code{future} multicore/sequential).}

\item{trait_rg_override}{Optional precomputed trait x trait rg matrix.}

\item{emit}{Logging function (default \code{\link[=message]{message()}}).}
}
\value{
A list with stacked \code{scores}/\code{labels}, the \code{primary} (z == primary) result, \code{zs}, and
per-z \code{meta}.
}
\description{
Re-runs \code{\link[=score_at_z]{score_at_z()}} over \code{sort(unique(c(z_vector, cfg$h2_z_threshold)))} in parallel over z
(the primary z is always folded in, so the primary slice always exists), stacks the two output
tables with a \code{z_threshold} column, pads every z's labels to the union cluster set, and flags
per-cluster \code{label_stable} (auto_label constant across all swept z).
}
