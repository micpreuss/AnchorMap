% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{score_at_z}
\alias{score_at_z}
\title{Score the engine at a single reliability threshold}
\usage{
score_at_z(df, ont, cfg, sroot, z, trait_rg_override = NULL, emit = message)
}
\arguments{
\item{df}{Gated long-table data frame (Input A schema).}

\item{ont}{Ontology data frame (read by \code{read_ontology()}).}

\item{cfg}{Config list (from \code{load_config()}).}

\item{sroot}{Stage root for resolving relative paths.}

\item{z}{Numeric h2-reliability threshold.}

\item{trait_rg_override}{Optional precomputed trait x trait rg matrix.}

\item{emit}{Logging function (default \code{\link[=message]{message()}}).}
}
\value{
A list with \code{ranked}, \code{labels}, and per-z metadata.
}
\description{
Runs the full single-z engine (gate -> redundancy -> score -> label) at one h2-reliability
threshold \code{z}. Deterministic: re-seeds with \code{cfg$random_seed} and preserves the exact
cluster x level loop order, so \code{z == cfg$h2_z_threshold} reproduces the single-z primary output
bit-for-bit.
}
