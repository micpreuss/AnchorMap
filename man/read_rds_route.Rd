% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest_rds.R
\name{read_rds_route}
\alias{read_rds_route}
\title{Assemble the engine inputs from a GenomicSEM \code{.rds}}
\usage{
read_rds_route(path, cfg, sroot, emit = message)
}
\arguments{
\item{path}{Path to a GenomicSEM \code{ldsc()} \code{.rds} file.}

\item{cfg}{Config list (from \code{load_config()}).}

\item{sroot}{Stage root for resolving relative paths.}

\item{emit}{Logging function (default \code{\link[=message]{message()}}).}
}
\value{
A list with \code{df}, \code{trait_rg}, \code{n_factors}, \code{n_panel}.
}
\description{
read -> partition (cluster factors vs panel traits) -> standardize -> delta-method \code{rg_se} ->
the Input-A long-table plus the trait x trait redundancy block, so the rest of the engine is
route-agnostic.
}
