% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{run_plots}
\alias{run_plots}
\title{Render the AnchorMap figures from scored TSVs}
\usage{
run_plots(
  config_path,
  q_sig = NULL,
  rg_floor = NULL,
  min_clusters = NULL,
  out_dir = NULL,
  in_dir = NULL
)
}
\arguments{
\item{config_path}{Path to a plot YAML config (or a bare shipped-config name).}

\item{q_sig, rg_floor, min_clusters}{Optional overrides of the significance gate.}

\item{out_dir}{Optional output-directory override (else \code{cfg$out_dir}).}

\item{in_dir}{Optional input-directory override: read each track's \code{scores}/\code{labels} from this
directory (by basename) instead of the config's paths, so figures can target any engine
\code{--out-dir}.}
}
\value{
Invisibly, the character vector of written file paths.
}
\description{
Reads a plot YAML config (\code{out_dir} + \verb{tracks: [\{name, level, scores, labels\}]}) and writes the
cluster-anchoring lollipops, dot-heatmap, AUC-vs-coherence scatter, and cross-cluster specificity
heatmap + diagonal (PNG + PDF) plus \code{cluster_distinctive_categories.tsv} into \code{out_dir}. Does not
re-run the engine - it only re-encodes the scored TSVs.
}
