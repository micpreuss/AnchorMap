% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_anchormap.R
\name{run_anchormap}
\alias{run_anchormap}
\title{Run the AnchorMap engine}
\usage{
run_anchormap(
  config_path,
  threads = 1L,
  rds = NULL,
  z_vector = NULL,
  out_dir = NULL,
  run_label = NULL,
  rg_long = NULL,
  trait_rg = NULL,
  ontology = NULL
)
}
\arguments{
\item{config_path}{Path to a YAML config, or a bare shipped-config name (e.g. \code{"synthetic_rds"}).}

\item{threads}{Worker/thread count (\code{setDTthreads} + the z-sweep workers).}

\item{rds}{Optional GenomicSEM \code{.rds} input (overrides \code{cfg$rds}).}

\item{z_vector}{Optional numeric vector overriding \code{cfg$z_vector} for the sweep.}

\item{out_dir}{Optional output-directory override (else \code{cfg$out_dir}, else \verb{results/<run_label>}).}

\item{run_label}{Optional run label (logging; fallback output dir when \code{out_dir} is unset).}

\item{rg_long, trait_rg, ontology}{Optional input-path overrides for the rg long-table, the
trait x trait rg matrix, and the ontology TSV (else taken from the config).}
}
\value{
Invisibly, a list with \code{ranked}, \code{labels}, \code{sens_scores}, \code{sens_labels}.
}
\description{
Drives the full engine from a YAML config: ingest (rg long-table TSV \strong{or} a GenomicSEM \code{.rds}),
reliability gate, within-category redundancy, competitive scoring, auto-labelling, and a parallel
reliability-threshold sensitivity sweep. Writes \code{category_anchor_scores.tsv},
\code{cluster_anchor_labels.tsv}, the two \verb{sensitivity_z_*} TSVs, and \code{anchormap.log} into the output
directory.
}
