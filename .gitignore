# Vendored scaffold — its own git repo, not part of AnchorMap
claude-science-scaffold/

# macOS
.DS_Store

# Machine-specific / private configs (absolute input paths) — kept out of the public package
local/

# R
.Rhistory
.RData
.Rapp.history
.Rproj.user/
*_cache/

# R package build artifacts + generated run outputs (engine TSVs/logs/figures — regenerable)
inst/results/
results/
*.tar.gz
*.Rcheck/

# Editor / OS
*~
.idea/

# Nextflow harness transient artifacts (Phase 5 — see nextflow/)
work/
.nextflow/
.nextflow.log*
trace-*.txt
report-*.html
timeline-*.html
